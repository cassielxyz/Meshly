import{notFound}from"next/navigation";import{SettingsView}from"@/components/workspace/settings-view";import{settingsTabs}from"@/lib/navigation";type Tab=(typeof settingsTabs)[number];function isTab(value:string):value is Tab{return(settingsTabs as readonly string[]).includes(value);}export default async function SettingsPage({params}:{params:Promise<{tab:string}>}){const{tab}=await params;if(!isTab(tab))notFound();return <SettingsView tab={tab}/>;}
