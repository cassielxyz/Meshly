import{NextRequest}from"next/server";import{and,eq,isNull,lt,or,sql}from"drizzle-orm";import{getDb}from"@/db/client";import{shares}from"@/db/schema";import{hashShareToken,verifyShareGrant}from"@/lib/security/share";import{streamLogicalFile}from"@/lib/storage/download";
export async function GET(request:NextRequest,{params}:{params:Promise<{token:string}>}){const{token}=await params;const db=getDb();const share=(await db.select().from(shares).where(eq(shares.tokenHash,hashShareToken(token))).limit(1))[0];if(!share||share.revokedAt||share.expiresAt&&share.expiresAt.getTime()<Date.now())return new Response("Not found",{status:404});if(share.passwordHash&&!verifyShareGrant(request.cookies.get("meshly_share_access")?.value,share.id))return new Response("Password required",{status:403});const updated=await db.update(shares).set({downloadCount:sql`${shares.downloadCount}+1`}).where(and(eq(shares.id,share.id),isNull(shares.revokedAt),or(isNull(shares.maxDownloads),lt(shares.downloadCount,shares.maxDownloads)))).returning({count:shares.downloadCount});if(!updated.length)return new Response("Download limit reached",{status:410});return streamLogicalFile(share.fileId,request.headers.get("range"));}
