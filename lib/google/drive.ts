export type DriveAbout={user?:{displayName?:string;emailAddress?:string;photoLink?:string};storageQuota?:{limit?:string;usage?:string;usageInDrive?:string;usageInDriveTrash?:string}};
export async function getDriveAbout(accessToken:string){const fields="user(displayName,emailAddress,photoLink),storageQuota(limit,usage,usageInDrive,usageInDriveTrash)";const response=await fetch(`https://www.googleapis.com/drive/v3/about?fields=${encodeURIComponent(fields)}`,{headers:{authorization:`Bearer ${accessToken}`},cache:"no-store"});if(!response.ok)throw new Error(`Drive quota request failed (${response.status})`);return response.json() as Promise<DriveAbout>;}
export async function ensureMeshlyFolder(accessToken:string){const q="name='Meshly Storage' and mimeType='application/vnd.google-apps.folder' and trashed=false";const list=await fetch(`https://www.googleapis.com/drive/v3/files?q=${encodeURIComponent(q)}&spaces=drive&fields=files(id,name)&pageSize=10`,{headers:{authorization:`Bearer ${accessToken}`},cache:"no-store"});if(!list.ok)throw new Error("Unable to inspect Meshly storage folder");const existing=await list.json() as {files?:{id:string}[]};if(existing.files?.[0]?.id)return existing.files[0].id;const create=await fetch("https://www.googleapis.com/drive/v3/files?fields=id",{method:"POST",headers:{authorization:`Bearer ${accessToken}`,"content-type":"application/json"},body:JSON.stringify({name:"Meshly Storage",mimeType:"application/vnd.google-apps.folder",appProperties:{meshly:"storage-root"}})});if(!create.ok)throw new Error("Unable to create Meshly storage folder");return ((await create.json()) as {id:string}).id;}
export async function createResumableSession(accessToken:string,input:{name:string;size:number;mimeType:string;parentId:string;appProperties?:Record<string,string>}){const response=await fetch("https://www.googleapis.com/upload/drive/v3/files?uploadType=resumable&fields=id,name,size",{method:"POST",headers:{authorization:`Bearer ${accessToken}`,"content-type":"application/json; charset=UTF-8","x-upload-content-type":input.mimeType,"x-upload-content-length":String(input.size)},body:JSON.stringify({name:input.name,parents:[input.parentId],mimeType:input.mimeType,appProperties:input.appProperties??{}})});if(!response.ok)throw new Error(`Unable to start Drive upload (${response.status})`);const url=response.headers.get("location");if(!url)throw new Error("Drive did not return a resumable session URL");return url;}
export async function getDriveFileMetadata(accessToken:string,fileId:string){const response=await fetch(`https://www.googleapis.com/drive/v3/files/${encodeURIComponent(fileId)}?fields=id,name,size,md5Checksum,sha256Checksum,trashed`,{headers:{authorization:`Bearer ${accessToken}`},cache:"no-store"});if(!response.ok)throw new Error(`Unable to verify Drive file (${response.status})`);return response.json() as Promise<{id:string;name:string;size?:string;md5Checksum?:string;sha256Checksum?:string;trashed?:boolean}>;}
export async function downloadDriveFile(accessToken:string,fileId:string,range?:string){return fetch(`https://www.googleapis.com/drive/v3/files/${encodeURIComponent(fileId)}?alt=media`,{headers:{authorization:`Bearer ${accessToken}`,...(range?{range}: {})},cache:"no-store"});}
