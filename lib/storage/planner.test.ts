import {describe,expect,it}from"vitest";import{planPlacement}from"./planner";const GiB=1024**3;describe("placement planner",()=>{it("keeps a file whole when a healthy node fits",()=>{const result=planPlacement(2*GiB,[{id:"a",freeBytes:5*GiB},{id:"b",freeBytes:9*GiB}],{reserveBytes:0});expect(result).toEqual([{accountId:"a",offset:0,size:2*GiB,part:0}]);});it("splits deterministically across accounts",()=>{const result=planPlacement(4*GiB,[{id:"a",freeBytes:2*GiB,priority:1},{id:"b",freeBytes:3*GiB,priority:2}],{reserveBytes:0,maxPartBytes:2*GiB});expect(result.map(x=>[x.accountId,x.offset,x.size])).toEqual([["a",0,2*GiB],["b",2*GiB,2*GiB]]);});it("can stripe even when one account fits",()=>{const result=planPlacement(3*GiB,[{id:"a",freeBytes:2*GiB,priority:1},{id:"b",freeBytes:8*GiB,priority:2}],{reserveBytes:0,maxPartBytes:2*GiB,wholeFileFirst:false});expect(result.length).toBe(2);expect(result[0]?.accountId).toBe("a");});it("rejects insufficient pooled capacity",()=>{expect(()=>planPlacement(10*GiB,[{id:"a",freeBytes:2*GiB}],{reserveBytes:0})).toThrow(/pooled storage/);});});
